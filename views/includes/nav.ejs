<nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top">
  <div class="container">

    <!-- Brand -->
    <a class="navbar-brand fw-bold fs-4 text-danger">
      letstravel
    </a>

    <!-- Mobile Toggle -->
    <button
      class="navbar-toggler border-0"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Nav Items -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Left links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if (user) { %>
          <% if(user.role === "USER"){ %>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="/">Home</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="/bookings">My Bookings</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="/user/profile">Profile</a>
            </li>
          <% } %>
          <% if(user.role === "HOST"){ %>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="/host/dashboard">Dashboard</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="/host/properties">My Properties</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="/host/properties/new">Add Property</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="#">Bookings</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-medium" href="/host/profile">Profile</a>
            </li>
          <% } %>
        <% } %>
      </ul>

      <!-- Right links -->
      <ul class="navbar-nav align-items-lg-center gap-lg-2">
        <% if (user) { %>
          <li class="nav-item">
            <a class="btn btn-outline-danger rounded-pill px-4" href="/logout">
              Logout
            </a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link fw-medium" href="/login">Login</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-danger rounded-pill px-4" href="/signup">
              Sign up
            </a>
          </li>
        <% } %>
      </ul>
    </div>

  </div>
</nav>
